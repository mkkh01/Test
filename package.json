{
  "name": "client-payment-scope-protection-platform",
  "version": "0.1.0",
  "private": true,
  "description": "A reusable template storefront and client discovery workspace.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "worker:discovery": "node src/workers/discovery-worker.js",
    "db:check": "node scripts/check-db.js",
    "check": "node --check server.js && node --check src/integrations/gemini-router.js && node --check src/integrations/telegram-bot.js && node --check src/integrations/usdt-verifier.js && node --check src/discovery/public-sources.js && node --check src/delivery/download-token.js && node --check src/workers/discovery-worker.js",
    "test": "node --test test/core.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0",
    "pg": "^8.23.0"
  }
}
